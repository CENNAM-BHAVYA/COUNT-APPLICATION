# COUNT-APPLICATION
This is a simple React application built using class components. The application demonstrates basic state management in React by implementing a counter with buttons to increment, decrement, and reset the count value.
